# Voting Ensemble Dedicated Inventory

Status: Ongoing item-by-item inventory. Each entry records whether the implementation is already dedicated to the Voting Ensemble algorithm or needs a dedicated Voting Ensemble copy so other trading algorithms are not affected.

## Allowed Shared Services

These services may remain shared and do not need dedicated Voting Ensemble copies.

| Service | Status | Shared implementation | Notes |
| --- | --- | --- | --- |
| Raw market-data collection | Shared allowed | `backend/app/collect_microstructure.py`; `backend/app/database.py`; `backend/app/main.py` | Raw market-data ingestion and candle storage remain shared infrastructure. `collect_microstructure.py` handles historical microstructure collection, `CandleStore` in `database.py` owns the shared candle table, and `main.py` initializes the shared store. Voting Ensemble should consume point-in-time market data through existing request/snapshot boundaries, but it does not need a duplicate raw market-data collector or candle database because raw collection is not algorithm-specific trading logic. |
| Alpaca API connection | Shared allowed | `backend/app/alpaca.py`; `backend/app/config.py`; `backend/app/main.py`; `backend/app/execution/paper_order_gateway.py` | Alpaca credentials, data/trading base URLs, market status, bar retrieval, and broker connectivity remain shared infrastructure. `AlpacaClient` centralizes Alpaca data/trading REST access and demo/local fallbacks, `Settings` owns the credential/base-url configuration, `main.py` initializes the shared client, and execution gateways consume broker protocols without requiring Voting Ensemble-specific API clients. Voting Ensemble may use shared Alpaca-fed market snapshots and broker gateways, but it does not need a duplicate Alpaca connection layer because API connectivity is not algorithm-specific voting logic. |
| Immutable candle storage | Shared allowed | `backend/app/database.py`; `backend/app/main.py` | Canonical candle storage remains shared infrastructure through `CandleStore` and the shared `candles` table keyed by provider, feed, symbol, timeframe, and timestamp. Voting Ensemble should read candles through shared market-data and point-in-time feature boundaries rather than owning a separate candle store. The current implementation uses idempotent upserts for the canonical candle cache; any stricter append-only or immutability hardening should be applied to the shared storage layer, not duplicated inside Voting Ensemble. |
| Economic calendar feed | Shared allowed | `backend/app/main.py`; `backend/app/alpaca.py`; `backend/app/algorithms/voting_ensemble/settings.py` | Economic-calendar and event-risk data remain shared market-context infrastructure. `main.py` exposes shared macro and Federal Reserve event feeds, `AlpacaClient` owns shared market-session calendar access and local calendar fallback, and Voting Ensemble consumes normalized event-risk inputs such as `eventRisk`, `eventRiskLevel`, and `newsRisk` through its dedicated trading-profile settings. Voting Ensemble does not need a duplicate economic-calendar feed because calendar events are shared context, not algorithm-specific voting logic. |
| News feed | Shared allowed | `backend/app/main.py`; `backend/app/config.py`; `backend/app/algorithms/voting_ensemble/settings.py` | News collection and headline-provider access remain shared market-context infrastructure. `main.py` owns `/api/news-feed`, `/api/news-summary`, Alpaca news retrieval, Yahoo Finance RSS fallback, provider status reporting, and fallback headline generation, while `Settings` owns shared Alpaca credential/base-url configuration. Voting Ensemble consumes normalized `newsRisk`/event-risk inputs through its dedicated trading-profile settings and should not duplicate the raw news feed because headline collection is not algorithm-specific voting logic. |
| Account snapshot | Shared allowed | `backend/app/risk/types.py`; `backend/app/execution/broker_reconciliation.py`; `backend/app/execution/paper_order_gateway.py`; `backend/app/algorithms/voting_ensemble/risk_budget.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py` | Broker/account snapshot retrieval and account-state contracts remain shared execution-risk infrastructure. `AccountSnapshot` defines the broker-authoritative equity, buying-power, freshness, and account-health fields, broker reconciliation refreshes snapshots before risk/order checks, and the paper gateway can synthesize a snapshot for local execution validation. Voting Ensemble keeps its algorithm-specific use of equity and account risk inside dedicated risk-budget and entry-policy modules, but it does not need a duplicate account snapshot feed because the account state is global broker context shared across algorithms. |
| Global portfolio-risk controls | Shared allowed | `backend/app/risk/manager.py`; `backend/app/risk/global_gate_engine.py`; `backend/app/risk/portfolio_gates.py`; `backend/app/risk/types.py`; `backend/app/execution/paper_order_gateway.py`; `backend/app/algorithms/voting_ensemble/gates.py` | Portfolio-wide risk controls remain shared account/execution safeguards. `GlobalPortfolioRiskManager` evaluates broker/account, market, portfolio, reservation, and order-integrity gates, the neutral global gate engine enforces account-wide quantity/exposure caps after an algorithm has produced a complete proposal, and the paper order gateway applies those shared decisions before broker submission. Voting Ensemble keeps its algorithm-specific local gates in `backend/app/algorithms/voting_ensemble/gates.py`, but it should not duplicate global portfolio-risk controls because account-wide exposure, buying-power, emergency-flatten, and reservation checks must remain consistent across all trading algorithms. |
| Broker adapter | Shared allowed | `backend/app/execution/broker_adapter.py`; `backend/app/execution/broker_reconciliation.py`; `backend/app/execution/paper_order_gateway.py`; `backend/app/algorithms/voting_ensemble/order_planner.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py` | Broker adapter and broker reconciliation boundaries remain shared execution infrastructure. `PaperBrokerAdapter`, `PaperBrokerClient`, `BrokerReconciliationEngine`, and `PaperOrderGateway` define the neutral broker protocol, idempotent submission, refreshed broker/account checks, order acknowledgements, fills, cancellation, and position/open-order refresh behavior. Voting Ensemble owns the algorithm-specific order proposal, entry policy, and order-planning logic before that boundary, but it should not duplicate broker adapters because broker connectivity and submission reconciliation must remain consistent across all trading algorithms. |
| Order transmission | Shared allowed | `backend/app/execution/paper_order_gateway.py`; `backend/app/execution/broker_reconciliation.py`; `backend/app/execution/simulation.py`; `backend/app/algorithms/voting_ensemble/order_planner.py` | Order transmission remains shared execution infrastructure after an algorithm emits an approved order plan/proposal. The shared gateway and broker reconciliation engine generate deterministic client order IDs, prevent duplicate submission, apply refreshed global risk and broker/account checks, submit orders to the broker adapter, reconcile acknowledgements/fills/protective orders, persist gateway snapshots, and cancel stale orders. Voting Ensemble owns its dedicated order-planning policy before this boundary, but it should not duplicate order transmission because submission idempotency, broker acknowledgement handling, fill reconciliation, and cancellation behavior must remain consistent across all trading algorithms. |
| Broker reconciliation | Shared allowed | `backend/app/execution/broker_reconciliation.py`; `backend/app/execution/paper_order_gateway.py`; `backend/app/gates/account_risk.py`; `backend/app/algorithms/voting_ensemble/order_planner.py` | Broker reconciliation remains shared execution safety infrastructure. `BrokerReconciliationEngine` owns the idempotent client-order ledger, refreshed broker/account/position/open-order inputs, refreshed global gate evaluation, broker acknowledgement/fill reconciliation, protective-order matching, and divergence detection such as `broker.local_broker_state_divergence`. Voting Ensemble owns the order plan/proposal that enters this boundary, but it should not duplicate broker reconciliation because pre-submit broker freshness, duplicate detection, fill reconciliation, and local-vs-broker divergence handling must stay consistent across all trading algorithms. |
| Logging and monitoring | Shared allowed | `backend/app/main.py`; `backend/app/execution/paper_order_gateway.py`; `backend/app/risk/persistence.py`; `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/strategy_performance.py` | Platform logging and monitoring remain shared observability infrastructure. `main.py` exposes the shared health endpoint, shared execution/risk services write gateway, fill, protective-order, and global-risk decision snapshots, and any process-level logging should stay cross-cutting. Voting Ensemble keeps algorithm-specific decision snapshots, performance metrics, reliability history, and report artifacts in its dedicated persistence and performance modules, but it should not duplicate platform logging/monitoring because service health, execution telemetry, and operational observability must remain consistent across all trading algorithms. |
| Generic database connection | Shared allowed | `backend/app/config.py`; `backend/app/database.py`; `backend/app/main.py`; `backend/app/algorithms/voting_ensemble/persistence.py` | Generic database configuration and SQLite connection/path handling remain shared infrastructure. `Settings.database_url` owns the configured database URL, `database.py` owns the local SQLite path helper and shared `CandleStore` connection behavior, and `main.py` initializes the shared store used by platform endpoints. Voting Ensemble may reuse the configured database and shared path parsing while keeping its algorithm-owned tables, migrations, envelopes, and repository methods in `backend/app/algorithms/voting_ensemble/persistence.py`; it should not duplicate the generic database connection because connection configuration is not algorithm-specific trading logic. |
| Generic backtest candle replay | Shared allowed | `backend/app/backtesting/event_replay.py`; `backend/app/backtesting/__init__.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/backtesting_adapter.py`; `backend/app/algorithms/voting_ensemble/backtest_config.py` | Generic event-driven candle replay remains shared backtesting infrastructure. `EventDrivenReplayEngine`, `ReplayComponents`, `ReplayEngineConfig`, replay snapshots, replay trades, and replay session contracts provide the reusable candle-by-candle execution loop and decision cadence. Voting Ensemble owns the algorithm-specific adapter, backtest configuration, injected strategy catalog, local gates, policy engine, order validator, execution simulator, session state, and candidate feature builder around that shared loop, but it should not duplicate generic replay mechanics because candle iteration, point-in-time replay contracts, and reusable replay result structures are not algorithm-specific voting logic. |

## Inventory

| Item | Status | Dedicated implementation | Notes |
| --- | --- | --- | --- |
| Voting Ensemble API | Dedicated | `backend/app/algorithms/voting_ensemble/api.py` | Core backend-authoritative API uses its own `APIRouter`, request model, service instance, `/api/voting-ensemble/evaluate`, and `/api/voting-ensemble/status`. Legacy dashboard/reporting endpoints under `/api/voting-ensemble/...` still live in `backend/app/main.py`, but they are route-specific to Voting Ensemble and do not expose a shared API used by other trading algorithms. |
| Voting Ensemble input/output contracts | Dedicated | `backend/app/algorithms/voting_ensemble/models.py` | Dedicated Pydantic contracts define `VotingCandle`, `VotingEnsembleEvaluateRequest`, `VotingStrategyVote`, `VotingContextConfirmation`, and `VotingEnsembleEvaluateResponse`. Voting Ensemble service/backtest code imports these contracts directly from the Voting Ensemble package. Other trading algorithms use their own contracts, such as Weighted Voting's `WeightedCandle`/`WeightedVotingMarketSnapshot` models, so no copy is required. |
| Dedicated strategy evaluators | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/strategies/directional/` | Backend-authoritative Voting Ensemble API/backtest evaluators are dedicated functions in `service.py`. The `V2TradingEngine` directional strategy class path now imports Voting Ensemble-owned copies of Multi-Timeframe Trend Alignment, First Pullback After Open, Failed Breakout Reversal, Liquidity Sweep Reversal, and Bollinger/ATR Reversion from `backend/app/algorithms/voting_ensemble/strategies/directional/`, leaving the shared `backend/app/strategies/directional/` modules untouched for other algorithms. |
| Strategy eligibility | Dedicated | `backend/app/algorithms/voting_ensemble/strategies/base.py`; `backend/app/algorithms/voting_ensemble/service.py` | Backend-authoritative Voting Ensemble votes set `eligible`/`dataReady` inside the dedicated `service.py`. The V2 trading-engine strategy copies now use Voting Ensemble-owned eligibility helpers for required-feature readiness, unavailable signals, hold signals, and eligible directional `StrategySignal` creation instead of importing `backend/app/strategies/base.py`. |
| Strategy reliability | Dedicated | `backend/app/algorithms/voting_ensemble/strategy_performance.py`; `backend/app/algorithms/voting_ensemble/service.py` | Backend-authoritative Voting Ensemble votes assign reliability inside dedicated `service.py`. The V2 First Pullback evaluator copy now uses `VotingEnsembleStrategyPerformanceTracker` and Voting Ensemble-owned `StrategyReliabilityEstimate` contracts instead of importing the shared `backend/app/domain/strategy_performance.py` tracker. Other copied directional evaluators keep their local reliability scoring methods inside the Voting Ensemble strategy package. |
| Regime-fit adjustment | Dedicated | `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py`; `backend/app/algorithms/voting_ensemble/service.py` | Backend-authoritative Voting Ensemble votes calculate `regimeFit` in dedicated `service.py`. The V2 replay path now uses a Voting Ensemble-owned family-aware ensemble copy, including the family-level `_family_regime_fit` multiplier that applies `trendFit`, `breakoutFit`, `reversalFit`, `meanReversionFit`, and `gapSessionFit` from `RegimeState` during aggregation. `V2TradingEngine` wires this dedicated ensemble copy without modifying the shared `backend/app/ensemble/family_aware.py`. |
| Strategy-family classification | Dedicated | `backend/app/algorithms/voting_ensemble/strategies/registry.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py` | Voting Ensemble now owns the family classification registry used by its copied strategy evaluators and dedicated family-aware ensemble. The registry includes the active V2 Voting Ensemble directional catalog and assigns Multi-Timeframe Trend Alignment/First Pullback to `TREND`, Failed Breakout/Liquidity Sweep to `REVERSAL`, Bollinger/ATR Reversion to `MEAN_REVERSION`, and the removed aggregator to `MARKET_CONTEXT`. Voting Ensemble no longer imports `backend/app/strategies/registry.py` for these classifications. |
| Family score aggregation | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py`; `backend/app/algorithms/voting_ensemble/ml_snapshots.py` | Backend-authoritative Voting Ensemble uses dedicated `_family_aware_decision`, `_family_scores`, and `_family_weights` functions in `service.py`. The V2 replay path uses the Voting Ensemble-owned family-aware ensemble copy for `FamilyAggregate`, `_family_aggregates`, `_weighted_family_mean`, `_family_support`, and `FamilyScore` output creation. Replay ML snapshot family-score feature extraction is already under the Voting Ensemble package in `ml_snapshots.py`. |
| Independent-family requirement | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py` | Backend-authoritative Voting Ensemble requires at least two supporting families before emitting Buy/Sell in `_family_aware_decision`. The V2 replay path uses the Voting Ensemble-owned family-aware ensemble config field `minimumIndependentSupportingFamilies` and emits `ensemble.insufficient_independent_family_support` from the dedicated copy when support is too narrow. |
| Context confirmation and conflict handling | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py`; `backend/app/algorithms/voting_ensemble/strategies/context/` | Backend-authoritative Voting Ensemble owns `_apply_context_to_candidate` and `_context_confirmation`, including double-conflict holds, single-conflict reductions, and explicit confirmation/weakening output. The V2 replay path uses the Voting Ensemble-owned family-aware ensemble copy for bounded `contextEffect` adjustments and `ensemble.context_conflict_exceeds_limit`. Active context evaluators, Relative Strength vs QQQ/IWM and Market Breadth Momentum, are now copied into the Voting Ensemble package so the `contextEffect` inputs consumed by conflict handling do not come from shared strategy modules. |
| Final Buy/Sell/Hold decision | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py`; `backend/app/api/trading_engine.py` | Backend-authoritative Voting Ensemble computes the final `Buy`/`Sell`/`Hold` value in dedicated `service.py` by combining `_family_aware_decision` with context confirmation/conflict handling before returning `final_signal`. The V2 replay path now uses the Voting Ensemble-owned family-aware ensemble copy, where `_decision_signal` converts the adjusted family score into the final `Signal.BUY`, `Signal.SELL`, or `Signal.HOLD`. `V2TradingEngine.vote_summary` only displays the decision coming from that dedicated replay ensemble wiring. |
| Decision explanations and reason codes | Dedicated | `backend/app/algorithms/voting_ensemble/service.py`; `backend/app/algorithms/voting_ensemble/models.py`; `backend/app/algorithms/voting_ensemble/ensemble/family_aware.py`; `backend/app/algorithms/voting_ensemble/strategies/` | Backend-authoritative Voting Ensemble owns response-level `reason_codes`, per-vote `reason`, and context adjustment explanations through dedicated service/model contracts. The V2 replay path now uses the Voting Ensemble-owned family-aware ensemble copy for final `reasonCodes` and `_explanation`, while copied strategy evaluators and context evaluators produce their own strategy-level explanations and blocker reason codes under the Voting Ensemble package. |
| Voting Ensemble baseline settings | Dedicated | `backend/app/algorithms/voting_ensemble/settings.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/backtest.py` | Voting Ensemble now owns a dedicated baseline settings module with its risk/session defaults, dynamic risk-setting normalization, settings version, and config hashing. `V2TradingEngine.dynamic_risk_config` uses this Voting Ensemble-owned module instead of the shared `backend/app/trading_policy/v1.py` wrapper. Dedicated backtest defaults remain in `VotingEnsembleBacktestConfig`; legacy V1 and older dashboard code can continue using their existing shared settings path without being affected. |
| Dynamic Voting Ensemble trading profile | Dedicated | `backend/app/algorithms/voting_ensemble/settings.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns deterministic dynamic trading-profile resolution through `resolve_dynamic_trading_profile` and `apply_dynamic_trading_profile`. The profile derives bounded effective risk, allocation, trade-count, slippage, and entry-blocking adjustments from Voting Ensemble inputs such as requested profile, volatility, liquidity, event risk, data quality, market regime, and drawdown. `V2TradingEngine.dynamic_risk_config` returns the Voting Ensemble-owned effective profile metadata and reason codes without mutating baseline settings or using Regime/WCA/Weighted Voting profile code. |
| Local Voting Ensemble gates | Dedicated | `backend/app/algorithms/voting_ensemble/gates.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns its local gate configuration, versioning, family scope, and replay gate engine wrapper through `VotingEnsembleLocalGateEngine`. The V2 replay engine injects this Voting Ensemble-owned gate engine instead of relying on the shared `ReplayComponents` default, so changes to Voting Ensemble local gate thresholds, reason-code prefixing, and gate metadata do not alter gate defaults used by other trading algorithms. The implementation still uses the shared gate input/output contracts for compatibility with the existing replay pipeline. |
| Voting-specific risk budget | Dedicated | `backend/app/algorithms/voting_ensemble/risk_budget.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/settings.py` | Voting Ensemble now owns its risk-budget and position-sizing calculation through `resolve_voting_ensemble_risk_budget` and `position_size_for_config`. `V2TradingEngine.position_size_for_config` uses the dedicated Voting Ensemble module instead of the shared `backend/app/execution/v1.py` wrapper. The dedicated budget honors Voting Ensemble effective settings, allocation mode, daily allocation limits, order risk-budget caps, risk-percent mode, and dynamic-profile `entriesBlocked` state without changing sizing behavior for V1 or other algorithms. |
| Vote-edge position sizing | Dedicated | `backend/app/algorithms/voting_ensemble/risk_budget.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns vote-edge sizing through `VOTING_ENSEMBLE_VOTE_EDGE_SIZING_VERSION` inside the dedicated risk-budget module. The sizing resolver can consume Voting Ensemble edge inputs such as `voteEdge`, `winnerEdge`, `voteStrength`, `finalScore`, `contextAdjustedScore`, `baseScore`, nested vote-summary/decision payloads, or confidence, then scales the approved risk/order budget by Voting Ensemble-specific edge bands. Missing edge preserves current sizing, while weak edge can zero-size the candidate without changing shared sizing behavior for other algorithms. |
| Entry policy | Dedicated | `backend/app/algorithms/voting_ensemble/entry_policy.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns its V2 replay entry policy and order validation through `VotingEnsembleReplayPolicyEngine` and `VotingEnsembleOrderValidator`. The V2 replay engine injects these dedicated components instead of the shared replay defaults, so entry-policy configuration hashes, gate/ML blocks, max quantity/notional logic, vote-edge caps, order-plan IDs, explanations, and validation codes can evolve for Voting Ensemble without changing entry behavior for other trading algorithms. |
| Order planner | Dedicated | `backend/app/algorithms/voting_ensemble/order_planner.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns order-plan construction through `VotingEnsembleOrderPlanner`. The dedicated planner builds accepted `LIMIT` plans, blocked `NO_ORDER` plans, max holding settings, quantity/notional caps, order-plan IDs, explanations, and planner-specific reason codes/configuration hashes. `VotingEnsembleOrderValidator` delegates to this planner while preserving the replay engine contract, so order-planning behavior can evolve for Voting Ensemble without changing the shared `DefaultReplayOrderValidator` or other algorithms. |
| Algorithm-owned position state | Dedicated | `backend/app/algorithms/voting_ensemble/position_state.py`; `backend/app/backtesting/event_replay.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns replay position-state attribution through `VotingEnsemblePositionState`. The replay engine accepts a `sessionStateFactory` component with the shared default unchanged, while V2 injects the Voting Ensemble state so active-position checks, session exposure rules, and broker snapshot positions are attributed to `voting_ensemble` instead of the generic replay algorithm ID. Other algorithms continue using the default `ReplaySessionState` unless they opt into their own factory. |
| Trade counter and cooldown state | Dedicated | `backend/app/algorithms/voting_ensemble/trade_counter_state.py`; `backend/app/backtesting/event_replay.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns replay trade-count, setup-count, duplicate-order, entry-cooldown, and stop-cooldown state through `VotingEnsembleTradeCounterState`. The replay engine routes session-rule checks through state methods with the shared defaults preserved, while V2 injects the Voting Ensemble state so trade counter and cooldown behavior can evolve for Voting Ensemble without changing other algorithms. |
| Voting Ensemble backtesting adapter | Dedicated | `backend/app/algorithms/voting_ensemble/backtesting_adapter.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns its API-facing backtesting adapter through `VotingEnsembleBacktestingAdapter`. The adapter normalizes candle requests, applies Voting Ensemble risk override mapping, chooses the symbol fallback, invokes the dedicated `VotingEnsembleBacktestRunner`, and stamps adapter version/reason codes on results. V2 routes backtest calls through this adapter instead of constructing the runner directly, while shared backtesting entry points remain unchanged for other algorithms. |
| Voting Ensemble backtest configuration | Dedicated | `backend/app/algorithms/voting_ensemble/backtest_config.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/algorithms/voting_ensemble/backtesting_adapter.py` | Voting Ensemble now owns backtest configuration through `VotingEnsembleBacktestConfig` in a dedicated config module. The config carries Voting Ensemble defaults for starting capital, warmup candles, stop/target distances, quantity, max holding period, execution simulation, decision-record controls, config version, and config reason codes. The dedicated runner and adapter consume this config module instead of embedding configuration inside runner code, so backtest defaults can evolve for Voting Ensemble without changing shared backtesting code or other algorithms. |
| Voting Ensemble candidate dataset | Dedicated | `backend/app/algorithms/voting_ensemble/candidate_dataset.py`; `backend/app/api/trading_engine.py`; `backend/app/backtesting/event_replay.py` | Voting Ensemble now owns its candidate dataset builder through `VotingEnsembleCandidateDatasetBuilder`. V2 injects the builder through the replay `featureBuilder` component and uses the Voting Ensemble candidate feature schema hash, while the shared replay default feature builder remains unchanged for other algorithms. The dedicated dataset emits decision-time-only ensemble, family, context, data-readiness, and market snapshot features with Voting Ensemble dataset version/reason codes and forbidden future/outcome fields explicitly checked. |
| ML feature schema | Dedicated | `backend/app/algorithms/voting_ensemble/ml_feature_schema.py`; `backend/app/algorithms/voting_ensemble/candidate_dataset.py`; `backend/app/api/trading_engine.py` | Voting Ensemble now owns its ML feature schema through `voting_ensemble_ml_feature_schema`. The schema module defines the Voting Ensemble candidate feature schema version, schema hash, feature names/specs, and schema reason codes. The dedicated candidate dataset builder consumes this schema and V2 uses the Voting Ensemble schema hash, while the shared `backend/app/ml/features.py` candidate meta-feature schema remains unchanged for other algorithms. |
| ML model and thresholds | Dedicated | `backend/app/algorithms/voting_ensemble/ml_model.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/ml_feature_schema.py` | Voting Ensemble now owns its ML model and threshold boundary through `voting_ensemble_ml_config`, `voting_ensemble_ml_model_artifact`, and Voting Ensemble-specific model/threshold versions. V2 injects the dedicated `SafeMLInferenceConfig` with a configuration hash bound to the Voting Ensemble candidate feature schema, probability thresholds, missingness/OOD/health gates, and risk-cap bounds. Shared ML inference code remains the execution contract, while other algorithms keep the shared default config. |
| Model calibration | Dedicated | `backend/app/algorithms/voting_ensemble/model_calibration.py`; `backend/app/algorithms/voting_ensemble/ml_model.py` | Voting Ensemble now owns its model calibration policy through `voting_ensemble_calibration_policy` and `voting_ensemble_model_calibration_artifact`. The calibration version is included in the Voting Ensemble ML configuration hash and artifact wrapper, with explicit requirements for out-of-fold calibration, probability-sizing approval, minimum calibration rows, and calibration metric caps. Shared calibration application remains in the common ML inference/training utilities, and other algorithms keep their existing calibration policy. |
| Walk-forward evaluation | Dedicated | `backend/app/algorithms/voting_ensemble/walk_forward.py`; `backend/app/algorithms/voting_ensemble/backtesting_adapter.py` | Voting Ensemble now owns chronological walk-forward evaluation through `VotingEnsembleWalkForwardEvaluator`. The dedicated module defines Voting Ensemble fold/config/result contracts, builds non-shuffled train/validation/test windows by session date, records fold boundaries and reproducibility keys, and runs unseen test windows through the dedicated Voting Ensemble backtesting adapter. Shared OOS helpers and other algorithms' walk-forward modules remain unchanged. |
| Parameter tuning | Dedicated | `backend/app/algorithms/voting_ensemble/parameter_tuning.py`; `backend/app/backtesting/parameter_tuning.py` | Voting Ensemble now owns its parameter-tuning boundary through `VotingEnsembleParameterTuningConfig` and `VotingEnsembleParameterTuningReport`. The dedicated wrapper stamps Voting Ensemble tuning version/reason codes, maps Voting Ensemble defaults into the shared robust-selection contracts, and preserves training-fold-only selection, outer-validation exclusion, stable-region preference, and frozen-before-final-test policy without changing parameter tuning behavior for other algorithms. |
| Performance tracking | Dedicated | `backend/app/algorithms/voting_ensemble/strategy_performance.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/algorithms/voting_ensemble/walk_forward.py` | Voting Ensemble now owns performance tracking through `VotingEnsembleStrategyPerformanceTracker`, `VotingEnsemblePerformanceSnapshot`, and strategy-level performance snapshots. The tracker keeps the existing Voting Ensemble reliability lookup and now adds versioned trade summaries, win-rate/expectancy/profit-factor/drawdown metrics, source-window metadata, and stage-result strategy rollups for walk-forward/reliability feedback. Shared strategy-performance tracking remains unchanged for other algorithms. |
| Strategy reliability history | Dedicated | `backend/app/algorithms/voting_ensemble/reliability_history.py`; `backend/app/algorithms/voting_ensemble/strategy_performance.py` | Voting Ensemble now owns strategy reliability history through `VotingEnsembleReliabilityHistory`, `VotingEnsembleReliabilityObservation`, and `VotingEnsembleReliabilityWindow`. The history module builds regime-scoped reliability windows from prior observations, provides neutral fallback behavior when history is unavailable, and feeds the existing Voting Ensemble performance tracker through `strategyReliabilityHistory` payloads without changing shared reliability estimators or other algorithms. |
| Strategy-family performance history | Dedicated | `backend/app/algorithms/voting_ensemble/family_performance_history.py`; `backend/app/algorithms/voting_ensemble/strategy_performance.py` | Voting Ensemble now owns family-level performance history through `VotingEnsembleFamilyPerformanceHistory`, family observations, and family performance windows. The dedicated history builds regime-scoped family performance scores from prior walk-forward/paper observations, provides neutral fallback behavior when family history is unavailable, and exposes family-window lookup through the Voting Ensemble performance tracker without changing shared family ensemble code or other algorithms. |
| Dedicated artifacts and reports | Dedicated | `backend/app/algorithms/voting_ensemble/artifacts.py` | Voting Ensemble now owns versioned artifact and report envelopes through `VotingEnsembleArtifactEnvelope` and `VotingEnsembleReportEnvelope`. The dedicated module provides stable content hashing, artifact/report IDs, supported Voting Ensemble artifact kinds, metadata, reason codes, and builder helpers for backtest, walk-forward, parameter-tuning, ML model, performance, reliability, and family-history outputs without changing shared report or artifact formats used by other algorithms. |
| Dedicated persistence inventory - Decision snapshots | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py` | Voting Ensemble now owns a dedicated persistence inventory and decision-snapshot storage boundary through `VotingEnsembleDecisionSnapshotEnvelope`, `voting_ensemble_persistence_inventory`, and `VotingEnsembleSqlitePersistenceRepository`. The module creates only the `voting_ensemble_decision_snapshots` table namespace, stamps snapshot/persistence versions, stores replay decision snapshots inside algorithm-owned envelopes with stable content hashes, and provides write/read/list/table-count helpers without changing shared replay snapshots, WCA repositories, or other algorithm persistence. |
| Dedicated persistence inventory - Strategy outputs | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py` | Voting Ensemble now owns per-strategy output persistence through `VotingEnsembleStrategyOutputEnvelope`, `build_voting_ensemble_strategy_outputs`, and the dedicated `voting_ensemble_strategy_outputs` table. Strategy outputs are extracted from Voting Ensemble replay decision snapshots into versioned algorithm-owned records with stable content hashes, strategy/family/signal lookup columns, and repository write/list helpers without changing shared `ReplayDecisionSnapshot`, WCA strategy-output tables, or other algorithm persistence. |
| Dedicated persistence inventory - Trades | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py` | Voting Ensemble now owns trade persistence through `VotingEnsembleTradeEnvelope`, `build_voting_ensemble_trades`, and the dedicated `voting_ensemble_trades` table. Replay trades are stored in versioned algorithm-owned ledger records with stable content hashes, decision-snapshot linkage, side/quantity/PnL lookup columns, and repository write/read/list helpers without changing shared `ReplayTrade`, WCA trade ledger tables, or other algorithm persistence. |
| Dedicated persistence inventory - Positions | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py` | Voting Ensemble now owns position persistence through `VotingEnsemblePositionEnvelope`, `build_voting_ensemble_position`, `build_voting_ensemble_positions_from_trades`, and the dedicated `voting_ensemble_positions` table. Position records are stored in versioned algorithm-owned envelopes with source, side, quantity, price, PnL, and source linkage columns, supporting replay-derived and broker/owned-position payloads without changing shared broker position state, virtual ownership ledgers, or other algorithm persistence. |
| Dedicated persistence inventory - Performance metrics | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/strategy_performance.py` | Voting Ensemble now owns performance metric persistence through `VotingEnsemblePerformanceMetricEnvelope`, `build_voting_ensemble_performance_metric`, and the dedicated `voting_ensemble_performance_metrics` table. Aggregate backtest, walk-forward, strategy, and reliability metrics can be stored in versioned algorithm-owned envelopes with source, scope, metric name/value, content hashes, and lookup columns without changing shared backtest outputs, WCA strategy-performance tables, or Voting Ensemble artifact/report envelopes. |
| Dedicated persistence inventory - Settings versions | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/settings.py` | Voting Ensemble now owns settings-version persistence through `VotingEnsembleSettingsVersionEnvelope`, `build_voting_ensemble_settings_version`, and the dedicated `voting_ensemble_settings_versions` table. Effective settings snapshots carry Voting Ensemble baseline/profile versions, profile ID, configuration hash, content hash, source, run, and optional symbol/session metadata without changing application config, WCA configuration-version tables, or other algorithm settings storage. |
| Dedicated persistence inventory - Model versions | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/ml_model.py`; `backend/app/algorithms/voting_ensemble/model_calibration.py` | Voting Ensemble now owns model-version persistence through `VotingEnsembleModelVersionEnvelope`, `build_voting_ensemble_model_version`, and the dedicated `voting_ensemble_model_versions` table. Model records capture Voting Ensemble model, threshold, calibration, feature-schema, configuration-hash, artifact ID, content hash, source, and run metadata without changing generic ML artifacts, settings-version storage, or other algorithm model state. |
| Dedicated persistence inventory - Dynamic-profile history | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/settings.py` | Voting Ensemble now owns dynamic-profile history persistence through `VotingEnsembleDynamicProfileHistoryEnvelope`, `build_voting_ensemble_dynamic_profile_history`, and the dedicated `voting_ensemble_dynamic_profile_history` table. Profile history records capture profile version, profile ID, active overlays, risk/allocation/daily-allocation/trade-count/slippage multipliers, entry-block state, source, run, session, and content hash without changing settings-version snapshots, WCA dynamic-profile state, or other algorithm profile history. |
| Dedicated persistence inventory - Backtest runs | Dedicated | `backend/app/algorithms/voting_ensemble/persistence.py`; `backend/app/algorithms/voting_ensemble/backtesting_adapter.py`; `backend/app/algorithms/voting_ensemble/backtest_config.py` | Voting Ensemble now owns backtest-run persistence through `VotingEnsembleBacktestRunEnvelope`, `build_voting_ensemble_backtest_run`, and the dedicated `voting_ensemble_backtest_runs` table. Run records capture run ID, symbol, timeframe, status, backtest/config/adapter versions, configuration hash, result hash, totals, config payload, and full result payload without changing WCA backtest-run tables, artifacts/reports, performance metrics, or shared backtest outputs. |
| Stop-loss policy | Dedicated | `backend/app/algorithms/voting_ensemble/stop_loss_policy.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py` | Voting Ensemble now owns its initial stop-loss policy version, default stop distance, minimum stop distance, stop-price calculation, and stop reason codes. The V2 replay config sources `defaultStopDistance` from the Voting Ensemble policy module, the dedicated backtest order plan uses `initial_stop_price`, and the dedicated entry policy includes the Voting Ensemble stop-loss policy version in its policy hash inputs. Other algorithms keep their existing shared stop/position-sizing policy behavior. |
| Profit-target policy | Dedicated | `backend/app/algorithms/voting_ensemble/profit_target_policy.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py` | Voting Ensemble now owns its profit-target policy version, default target distance, default target R, minimum target distance, target-price calculation, and target reason codes. The V2 replay config sources `defaultTargetDistance` from the Voting Ensemble policy module, the dedicated backtest order plan uses `initial_target_price`, and the dedicated entry policy includes the Voting Ensemble profit-target policy version in its policy hash inputs. Other algorithms keep their existing shared target/exit policy behavior. |
| Exit policy | Dedicated | `backend/app/algorithms/voting_ensemble/exit_policy.py`; `backend/app/api/trading_engine.py`; `backend/app/algorithms/voting_ensemble/backtest.py`; `backend/app/algorithms/voting_ensemble/entry_policy.py` | Voting Ensemble now owns its exit-policy version, execution simulation config, default max holding period, and exit reason-code prefixing through `VotingEnsembleExecutionSimulator`. The V2 replay engine injects the dedicated simulator, the dedicated backtest uses the Voting Ensemble execution config and max holding default, and the dedicated entry policy stamps order plans with the Voting Ensemble max holding period and exit policy hash. Other algorithms keep their existing shared execution simulator defaults. |
